name: CIWorkflow
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
      - uses: actions/checkout@v4
        
      - name: Set up Docker
        uses: docker/setup-buildx-action@v4
        